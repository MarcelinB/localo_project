{% extends 'base.html.twig' %}

{% block title %}Ma ferme
{% endblock %}

{% block body %}
	<div class="flex flex-row relative  min-h-screen  bg-gradient-to-r from-rose-100 to-teal-100">
		{% include "component/sidebar_farm.html.twig" %}
		<div class="container mx-auto mt-10 max-w-2xl">
        <img src="{{ asset('uploads/' ~ farm.image) }}" class="mw-100" alt="{{ farm.name }}">
        </div>

	</div>

</div>{% endblock %}
